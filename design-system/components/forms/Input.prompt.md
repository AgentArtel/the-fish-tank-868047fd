Labelled text input with coral focus ring, optional leading icon, and hint/error text. Use for search, forms, and filters.

```jsx
<Input label="Email" type="email" placeholder="you@example.com" hint="We'll text you when it's ready." />
<Input leftIcon={<Search />} placeholder="Search by name or scientific name…" />
<Input label="Name" error="Required" />
```
